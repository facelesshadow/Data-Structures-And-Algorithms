#include <iostream>
#include <vector>



int main(){
    std::vector<int> name = {1, 2};
    for(int i = 0; i < name.size(); i++)
    {
        std::cout << name.size() << std::endl;
    }
    name.push_back(92);
    std::cout << cname.end() << std::endl;
    return 1;
}